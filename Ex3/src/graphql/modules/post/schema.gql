type Post {
  id: Int
  title: String
  content: String
  author: Author
}

type Query {
  listPost: [Post]
  post(id: Int!): Post
}

type Mutation {
  createPost(title: String!, content: String!, author: String!): Post
}
